package com.qitracker.domain;

// Lowercase to match the convention used by the other enums in this app (Direction, ElementSign, etc.)
public enum PdsaActDecision { in_progress, adopt, adapt, abandon }